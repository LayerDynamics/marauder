{
  "name": "{{name}}",
  "version": "0.1.0",
  "description": "{{description}}",
  "entry": "mod.ts",
  "permissions": ["events", "commands", "notifications"],
  "engines": {
    "marauder": "^1.0.0"
  }
}
